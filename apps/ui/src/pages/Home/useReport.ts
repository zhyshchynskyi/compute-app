const useReport = () => {
  // const { data } = useTotalMonthlyUsageService()
  // const { data: subnet_usages } = useSubnetUsageService()

  return {
    monthly_usage: [],
    subnet_usages: {},
  }
}

export default useReport
